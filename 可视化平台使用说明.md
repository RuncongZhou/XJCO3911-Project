# 多设备DNN协作推理可视化平台 — 使用指南

## 概览

本平台用于演示多设备深度神经网络（DNN）协作推理流程，主要功能包括：

1. **模型层次可视化** — 展示DNN模型的层结构与参数
2. **设备网络拓扑** — 展示边缘设备间的连接与带宽
3. **分割方案交互** — 支持自定义设备顺序并查看自动分割结果
4. **性能分析** — 展示吞吐量、推理时间等指标
5. **算法对比** — 对比不同分布式推理算法的表现

## 安装与启动

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 数据文件

确保 `data/` 目录下包含以下 CSV 文件：
- AlexNet.csv, Vgg19.csv, YOLONet.csv
- Resnet50.csv, Resnet101.csv, SqueezeNet.csv

### 3. 启动服务

```bash
python app.py
```

服务将在 `http://localhost:5000` 启动。

## 功能说明

### 模型配置
- **模型选择**：AlexNet、VGG19、YOLONet 等
- **设备数量**：2–10 台
- **算法**：HiveMind 或 EdgePipe

### 设备配置
- **带宽范围**：MB/s
- **性能范围**：GFlops/s
- **生成拓扑**：随机生成设备网络

### 设备顺序
- 拖拽标签调整顺序
- 随机排序按钮

### 可视化标签
- **模型结构**：层结构、类型、颜色区分
- **设备拓扑**：连接关系、带宽
- **分割方案**：各设备负责的层
- **性能指标**：吞吐量、推理时间、利用率

### 模拟与对比
- 执行模拟：根据配置计算分割与性能
- 算法对比：同时运行多种算法并比较

## 技术栈

- **后端**：Flask, NumPy, Pandas
- **前端**：D3.js, vis.js, 原生 JavaScript
- **算法**：HiveMind（增强 Dijkstra）、EdgePipe（动态规划）

## 注意事项

1. 首次加载模型数据可能较慢
2. 设备数量多时计算时间增加
3. 部分配置可能无可行解
4. 建议使用 Chrome、Firefox、Edge

## 故障排除

- **无法启动**：检查 Python 版本（3.8+）、依赖、端口 5000
- **数据加载失败**：确认 data/ 下 CSV 存在且格式正确
- **模拟失败**：检查设备数量、带宽与性能范围
