# 实验结果对比指南

## 一、平台内快速对比（单次）

1. 启动服务：`python app.py`
2. 打开 http://127.0.0.1:5000
3. 在左侧配置：模型、设备数、带宽范围、性能范围、设备顺序
4. 点击 **Compare algorithms**
5. 切换到 **Compare** 标签查看 HiveMind vs EdgePipe 的吞吐量和推理时间
6. 点击 **Export to CSV** 导出当前对比结果

## 二、批量实验（系统化收集数据）

用于论文中多组实验的对比。

1. 启动服务：`python app.py`（保持运行）
2. 新开终端运行：`python run_batch_experiments.py`
3. 脚本会依次执行多组配置，结果保存到 `experiments_results.csv`
4. 用 Excel 或 Python 分析 CSV，绘制对比图表

### 实验维度（可自行修改 run_batch_experiments.py）

- **不同模型**：AlexNet、Vgg19、YOLONet、SqueezeNet
- **不同设备数**：3、4、5、6
- **不同带宽**：10–20、21–31、40–60 MB/s
- **不同算力**：20–40、41–60、60–100 GFlops/s

## 三、结果分析建议

1. **表格**：在论文中可做成 HiveMind vs EdgePipe 的对比表
2. **折线图**：横轴为设备数/带宽/算力，纵轴为吞吐量或推理时间
3. **柱状图**：不同模型下两种算法的性能对比

## 四、自定义实验配置

编辑 `run_batch_experiments.py` 中的 `EXPERIMENT_CONFIGS`，按需增删或修改配置。
